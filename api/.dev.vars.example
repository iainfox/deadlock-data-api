# For `wrangler dev` the R2 binding uses your account credentials, so set:
#   CLOUDFLARE_API_TOKEN=...
#   CLOUDFLARE_ACCOUNT_ID=...
# in the environment (or a local .env / .dev.vars if you prefer).
